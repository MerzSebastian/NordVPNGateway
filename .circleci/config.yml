version: 2.1

jobs:
  build-old:
    docker:
      - image: docker:latest
    steps:
      - checkout
      - setup_remote_docker
      - run: cd nordvpn-old
      - run: docker build -t nordvpn-old ./nordvpn-old
  build-new:
    docker:
      - image: docker:latest
    steps:
      - checkout
      - setup_remote_docker
      - run: cd nordvpn-old
      - run: docker build -t nordvpn-new ./nordvpn-new


workflows:
  build_and_test:
    jobs:
      - build-old
      - build-new